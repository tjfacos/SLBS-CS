C = imread("Redang.jpg");

figure(1);
imshow(C);

W = C;

W(:,:,1) = ( C(:,:,1) + C(:,:,2) + C(:,:,3) )/3;
W(:,:,2) = ( C(:,:,1) + C(:,:,2) + C(:,:,3) )/3;
W(:,:,3) = ( C(:,:,1) + C(:,:,2) + C(:,:,3) )/3;

figure(2);
imshow(W);